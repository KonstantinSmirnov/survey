<%
  @question = Question.find(params[:question_id])
  form_for [:app, @question.survey, @question] do |f|
    @question_form = f
  end
%>

$("#question-<%= @question.id %>-image").html("<%= escape_javascript(render partial: 'question_image', locals: { f: @question_form }) %>");
