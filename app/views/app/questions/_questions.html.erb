<% @survey.questions.each do |question| %>
  <div id="question-<%= question.id %>" class="question-form">
    <%= render partial: 'app/questions/question', locals: { question: question } %>
  </div>
<% end %>
