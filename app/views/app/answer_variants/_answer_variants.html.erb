<fieldset class="form-group">
  <% question.answer_variants.each do |answer_variant| %>
    <%= render partial: 'app/answer_variants/answer_variant', locals: { answer_variant: answer_variant } %>
  <% end %>
</fieldset>
